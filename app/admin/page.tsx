export default function AdminPage() {
  return (
    <section className="section">
      <h2>Admin Portal</h2>
      <p>
        This is the admin landing route. Add additional admin tools under
        <code> /admin/* </code> as needed.
      </p>
    </section>
  );
}
